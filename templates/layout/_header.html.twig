<header class="navbar navbar-dark bg-primary sticky-top flex-md-nowrap p-0 shadow">
    <a class="navbar-brand app-brand me-0 px-3 py-3 fw-semibold" href="{{ path('app_dashboard') }}">
        <i class="bi bi-shield-lock me-2"></i>Afriocean fish Gestion
    </a>
    <button class="navbar-toggler d-lg-none me-3" type="button" data-bs-toggle="offcanvas" data-bs-target="#mobileSidebar" aria-controls="mobileSidebar" aria-label="Ouvrir le menu">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="d-none d-lg-flex align-items-center ms-auto px-3 text-white">
        <i class="bi bi-person-circle me-2"></i>
        <span class="me-3">{{ app.user.displayName }}</span>
        <a class="btn btn-outline-light btn-sm" href="{{ path('app_logout') }}">
            <i class="bi bi-box-arrow-right me-1"></i>Déconnexion
        </a>
    </div>
</header>
